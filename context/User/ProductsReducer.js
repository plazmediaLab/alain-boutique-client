import {} from '../types';

export default function ProductsReducer(state, action) {
  switch (action.type) {
    // case AUTH_LOGIN:
    //   return {
    //     ...state,
    //     user: action.payload,
    //     logged: true
    //   };

    default:
      return state;
  }
}
